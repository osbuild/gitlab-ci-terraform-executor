#!/bin/bash

set -x

currentDir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source "${currentDir}/base.sh"

$TERRAFORM destroy -auto-approve

VM_IP=$(cat "${TERRAFORM_JOB}/ip")
ssh-keygen -R "$VM_IP"

rm -rf "$TERRAFORM_JOB"
